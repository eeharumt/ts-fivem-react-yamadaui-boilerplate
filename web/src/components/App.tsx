import { FC } from "react";
import { Button, Container } from "@yamada-ui/react";

const App: FC = () => {
  return (
    <Container w="full" h="100vh" centerContent>
      <Button>Click me!</Button>
    </Container>
  );
};

export default App;
